public class Simulation {
  private GameObject[] objects;

  Simulation(int num) {
    objects = generateNRandomObjects(num);
  }

  private GameObject[] generateNRandomObjects(int num) {
    GameObject[] objects = new GameObject[num];
    for(int i = 0; i < num; i++) {
      objects[i] = new
    }
  }

  public runSimulation() {

  }
}

interface Constants {
  int GAME_SIZE = 500;

}
